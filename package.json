{
  "name": "p1asm0",
  "private": true,
  "workspaces": [
    "packages/*",
    "templates/*",
    "extensions/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "patch": "pnpm --filter ./packages/** --parallel -r exec pnpm version -- patch",
    "publish": "pnpm -r publish --parallel --filter ./packages/**"
  },
  "devDependencies": {
    "turbo": "1.2.6"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=16.0.0"
  },
  "packageManager": "pnpm@7.0.0",
  "version": "0.0.2"
}
